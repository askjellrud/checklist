// import { create } from "zustand";
// import { Check } from "../checklists/NewCheck";

// type Store = {
//   check: Check | null;
//   setCheck: (check: Check) => void;
// };

// export const useCheckResponseStore = create<Store>((set) => ({
//   check: null,
//   setCheck: (check: Check) =>
//     set((state) => {
//       return { ...state, check };
//     }),
// }));
